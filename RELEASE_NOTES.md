# 🚀 NextKey v1.1.1

**✨ Tính năng mới:**
---
- **Công cụ chuyển mã (Convert Tool):**
    - Thêm tính năng **bôi đen** để chuyển mã nhanh.
    - Thêm tùy chọn **chuyển mã tuần tự**: Tự động chuyển đổi qua lại giữa các tùy chọn sau đó về lại ban đầu (thứ tự từ trên xuống dưới, từ trái qua phải).
        - *Lưu ý:* Chỉ hoạt động khi tùy chọn "Tự động dán + bôi đen" được bật.
    - *Một số hạn chế trong phiên bản này:*
        - Chỉ hỗ trợ số lượng ký tự nhất định (nếu vượt quá sẽ chỉ tự động dán thay vì bôi đen lại).
        - Chỉ hỗ trợ chuyển đổi trong cùng bảng mã, khác bảng mã có thể phát sinh lỗi.
        - Một số ứng dụng có thể chưa tương thích hoàn toàn.

**🐛 Sửa lỗi:**
---
- Sửa lỗi không gõ được dấu (ví dụ: sức-swcs, sữa-swax) khi dùng Telex kết hợp với "Kiểm tra chính tả" và "Tự động sửa từ".
- Sửa lỗi cài đặt của công cụ chuyển mã (Convert Tool) không được lưu đúng cách.
- Khắc phục xung đột phím tắt giữa **Chuyển mã nhanh** và **Chuyển chế độ gõ** (ví dụ: `Ctrl + Shift + X` cho convert tool và `Ctrl + Shift` cho chuyển chế độ gõ sẽ được apply đồng thời).
- Sửa lỗi chuyển mã trên Word 365 hiển thị sai (ví dụ: "xin chào tết" -> "Xin chao tet" thay vì "xin Chao tet").

---
Cảm ơn bạn đã lựa chọn NeXTKey!
